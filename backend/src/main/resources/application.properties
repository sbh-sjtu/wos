spring.application.name=backEnd
server.port=8888

mybatis.type-aliases-package=com.example.backend.model
mybatis.mapper-locations=classpath:mapper/*.xml

# SQL Server Configuration?
spring.datasource.url=jdbc:sqlserver://10.119.9.41:1433;databaseName=wos_test;trustServerCertificate=true;encrypt=false
spring.datasource.username=wos
spring.datasource.password=wostest@120
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Connection pool settings for SQL Server
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# ElasticSearch Configuration
spring.elasticsearch.uris=http://localhost:9200
elasticsearch.enabled=true

# CORS Configuration
spring.mvc.cors.allowed-origins=http://localhost:3000
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*
spring.mvc.cors.allow-credentials=true

# Logging Configuration
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE